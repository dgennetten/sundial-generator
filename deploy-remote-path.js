/**
 * Production SFTP document root for precisionsundial.com.
 * Fixed path — not read from environment (avoids accidental wrong-target deploys).
 */
export const DEPLOY_REMOTE_ROOT = '/home/dgennetten/precisionsundial.com';
